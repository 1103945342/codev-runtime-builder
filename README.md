# CodeV Runtime Builder

This small repository builds the source-generated `com.codev` Bootstrap and
publishes the matching APT repository to GitHub Pages. The generated runtime
artifact is also uploaded to the workflow run.

Run **Actions → Build CodeV Runtime → Run workflow**.
